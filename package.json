{
  "name": "offline-secure-session",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Pure offline static webpage for verified ECDH session establishment and AES-GCM secret messaging.",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "serve": "python3 -m http.server 4173 --bind 127.0.0.1"
  }
}
